{% extends 'base.html.twig' %}

{% block body %}
    <h3>Inscription<h3/>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success"> Inscription <button/>
    {{ form_end(form) }}
{% endblock %}